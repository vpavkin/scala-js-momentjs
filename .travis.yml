language: scala
scala:
  - 2.12.10
  - 2.13.1

dist: trusty
group: edge
sudo: required

jdk:
  - oraclejdk8

env:
  - SCALAJS_VERSION=0.6.32
  - SCALAJS_VERSION=1.0.0-RC2

cache:
  directories:
    - $HOME/.ivy2/cache
    - $HOME/.sbt/boot

script:
  - sbt ++$TRAVIS_SCALA_VERSION! clean test
