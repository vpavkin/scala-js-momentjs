language: scala
scala:
  - 2.12.8
  - 2.13.0

dist: trusty
group: edge
sudo: required

jdk:
  - oraclejdk8

env:
  - SCALAJS_VERSION=0.6.31
  - SCALAJS_VERSION=1.0.0-M8

cache:
  directories:
    - $HOME/.ivy2/cache
    - $HOME/.sbt/boot

script:
  - sbt ++$TRAVIS_SCALA_VERSION! clean test
