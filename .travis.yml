language: scala
scala:
  - 2.12.11
  - 2.13.2

dist: trusty
group: edge
sudo: required

jdk:
  - oraclejdk8

env:
  - SCALAJS_VERSION=0.6.32
  - SCALAJS_VERSION=1.0.1

cache:
  directories:
    - $HOME/.ivy2/cache
    - $HOME/.sbt/boot

script:
  - sbt ++$TRAVIS_SCALA_VERSION! clean test
